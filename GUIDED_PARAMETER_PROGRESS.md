# 可解释参数引导与调试闭环项目进度

更新时间：2026-08-13

## 当前状态

计划中的主体闭环已经实现：用户可以理解参数含义和当前配置事实，在字段旁与运行前确认窗口查看结构化风险证据，应用只涉及数值控制项的建议，并从降级的数值健康报告创建可追溯的对照实验。界面没有增加“体验模式 / 专业模式”，也没有重做已有初始配置。

## 已完成

- 配置校验新增权威 `configSummary`：预计积分步数、模拟覆盖时间、预计首个结束条件、初始最近天体对与距离、软化长度比例。
- 风险问题新增结构化 `guidance`：观察、影响、数值证据、首选动作、代价和替代动作。
- `TIME_STEP_TOO_LARGE` 的建议步长由后端按局部周期比例和单步位移比例计算，并保留 10% 余量。
- 时间步长、软化长度、结束条件、质量、位置、速度和引力常数均提供常驻短句与可展开说明。
- 本地输入合法后 500 ms 自动请求服务端校验；旧响应按校验代次丢弃；创建前强制重新校验。
- 参数字段旁与 Warning 弹窗复用同一份后端引导；用户可应用建议，也可理解风险后按原配置运行。
- 自动 patch 只修改时间步长，以及为保持覆盖时间联动的最大步数；不自动修改软化、质量、位置、速度或引力常数。
- 对照实验规划覆盖 `MAX_STEPS`、`TARGET_TIME`、`BOTH`，展示前后步长、步数、覆盖时间和相对计算量。
- 保持覆盖时间超过 100,000,000 步时，要求用户选择缩短覆盖时间、采用最小合法步长或取消手动修改。
- 创建请求新增可信最小 `retryContext`；服务端验证源运行记录和当前 Health 建议，生成来源深度与配置差异快照。
- 来源关系随运行记录持久化；队列显示第 N 次对照实验，报告显示来源、关键差异和源/当前 Health；删除来源不级联删除后代。
- 用户在接受建议后又修改软化或初始条件时，界面提示结果差异不能只归因于时间步长。
- Health 首层改为“数值结果可信度”，按“结论—证据—怎么验证—技术详情”展示，并明确 FAILED 不等于星系解体。
- Health 建议新增收益、代价和验证方法；主操作改称“创建对照实验”。
- 事件面板新增“现象总览”，严格区分“已观察到”“可能相关”“如何确认”；报告页复用同一组件。
- 修复诊断证据中时间步长误用长度单位格式的问题。
- 队列 Health badge 使用中文结论，不把英文状态码作为主文案。
- OpenAPI、WebSocket Schema、生成的 TypeScript 类型和离线 mock 已同步。

## 已完成验证

- `mvn -pl simulation-web -am test`：Core 28、Application 77、Web 15，共 120 项通过。
- 前端定向测试：14/14 通过，覆盖三种结束条件、一亿步冲突、防抖校验、乱序响应和 Health 权威展示。
- Vue TypeScript 类型检查通过。
- `npm run build` 通过；保留既有 3D chunk 超过 500 kB 警告。
- 完整前端单测：151/153 通过；剩余两项为现有 jsdom Canvas/WebGL 环境限制。
- Playwright E2E：11/12 通过；Warning 提交、观察模式、暂停继续、1920×1080 四卡和响应式均通过。剩余一项为现有 tooltip 测试在固定画布中心未命中天体。
- 本地最新服务已在 `http://127.0.0.1:8723` 启动：首页和实验列表返回 200；结构化配置摘要与建议 REST 冒烟通过。

## 最终验证限制

- `mvn verify` 已通过 Core、Application、Web 和 Swing；Launcher 在执行 `npm ci` 时受 Windows `spawn EPERM` 中止。随后已在沙箱外成功恢复 `npm ci` 并单独完成前端生产构建。
- 项目没有 lint 脚本，因此 lint 未运行。
- 最终 `git diff --check` 已通过；任务文件 diff 和工作区状态已审阅，未发现意外生成物或越界修改。

## 工作区边界

没有回退或覆盖用户原有的 README、docs、配置文件、`frontend/public/mockServiceWorker.js` 及其他无关改动；本轮只创建 WIP 提交，不创建分支或推送。
