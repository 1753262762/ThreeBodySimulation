# 可解释参数引导与调试闭环实施计划

更新时间:2026-08-13

> 实施状态:主体闭环已于 2026-08-13 落地.当前验证与剩余环境限制见 `GUIDED_PARAMETER_PROGRESS.md`.

## 一、目标与成功标准

在现有单一界面上建立完整的用户调试闭环:

```text
理解参数
  → 看到当前配置的实际含义
  → 理解运行风险
  → 运行后区分数值问题与物理现象
  → 创建可追溯的对照实验
  → 比较关键参数与最终数值健康
```

本轮不增加"体验模式 / 专业模式",不重复建设已有的初始配置或场景卡片,也不把用户视为缺乏理解能力.系统应在用户需要作出决定的位置,用平实、尊重、证据优先的语言解释:

- 参数控制什么.
- 当前配置意味着什么.
- 风险或失败为什么发生.
- 观察到的星系解体是物理现象、数值问题,还是暂时无法判断.
- 下一次对照实验应该修改什么,以及修改的收益和代价.

成功标准是:用户能够理解主要参数的作用,解释模拟为何数值失败,并根据系统给出的证据创建下一次对照实验,而不是只会接受一个不可解释的自动修复结果.

## 二、参数与运行前引导

### 2.1 参数说明范围

所有参数继续保留在同一个参数编辑器中:

- 时间步长、软化长度、结束条件提供完整的"作用—风险—怎么调"说明.
- 质量、初始位置、初始速度和引力常数提供较短的因果说明.
- 不新增第二套参数界面或用户模式.

每个字段采用"常驻短句 + 按需展开"的形式:

- 常驻短句说明参数最核心的作用.
- 展开区域说明设置不合适可能造成的结果,以及在观察到什么现象时应该调整.
- Tooltip 可以保留为辅助,但不再承载用户作出决定所必需的信息.

### 2.2 当前配置解读

参数区常驻显示可以确定计算的事实:

- 预计积分步数.
- 预计模拟覆盖时间.
- 同时设置最大步数和目标时间时,预计首先触发的结束条件.
- 初始最近天体对及距离.
- 软化长度与初始最近距离的比例.

系统只有在检测到实际风险时才主动显示调参建议;安全配置不持续显示无关建议.

### 2.3 自动校验时机

- 本地输入格式合法后,用户停止输入 500 ms 自动调用后端 `/configs/validate`.
- 每次请求携带前端递增的校验代次;前端丢弃晚于新请求返回的旧响应.
- 输入仍不完整或本地格式非法时不请求后端.
- 用户点击创建实验时必须再次执行服务端校验,不能直接复用防抖校验结果.

### 2.4 风险解释位置

风险解释同时出现在两个位置:

1. 对应字段附近,帮助用户边修改边理解.
2. 现有 Warning 确认弹窗中,启动前集中复核所有风险.

每项风险按以下顺序展示:

1. 当前观察和证据.
2. 可能影响.
3. 首选调整方案.
4. 调整方案的代价.
5. 可选的替代方案.

Warning 弹窗突出"应用建议后再运行",同时保留"理解风险,仍按原配置运行".HIGH 风险不强制用户修改,避免剥夺其主动研究不稳定或非束缚系统的自由.

### 2.5 自动修改边界

系统只允许一键应用数值控制项:

- 时间步长.
- 为保持相同模拟覆盖时间而联动调整的最大步数.

以下参数只提供证据与人工调整建议,不自动修改:

- 软化长度,因为它会改变近距离物理模型.
- 初始位置和初始速度,因为它们会改变研究问题.
- 质量和引力常数,因为它们会改变系统本身.

`TIME_STEP_TOO_LARGE` 的运行前建议值由后端根据当前最严重的局部周期比和单步位移比计算,使两项回到 CAUTION 阈值以内并保留 10% 余量.运行后的 Simulation Health 降级继续使用现有 `currentDt / 10` 建议规则.

## 三、数值健康解释

### 3.1 首层语义

Health 首层统一称为"数值结果可信度",明确其边界:

- 只评价积分计算的数值误差和数值失败.
- 不保证初始模型能够代表真实宇宙.
- 不把天体逃逸或系统解体自动视为错误.

四个状态的用户文案为:

| 状态 | 首层文案 |
|---|---|
| `GOOD` | 数值结果可信度良好 |
| `WARNING` | 数值结果需要谨慎解读 |
| `POOR` | 数值误差已经明显 |
| `FAILED` | 计算在失败点之后不可用 |

后端仍是状态判级的唯一权威,前端不重新计算阈值或等级.

### 3.2 信息层级

Health 展开后固定按四层展示:

1. **结论**:发生了什么.
2. **证据**:漂移峰值、峰值时间、相关近遇、失败天体和字段.
3. **怎么验证**:建议创建怎样的对照实验,以及收益和代价.
4. **技术详情**:阈值、采样步长、样本数、趋势、reason code 和原始数值.

保留"能量漂移、角动量漂移、软化长度"等术语,但首次出现时随文解释其判断作用.例如,能量漂移用于观察计算过程中本应近似守恒的系统能量是否发生了不可忽略的数值变化.

### 3.3 FAILED 说明

FAILED 必须明确区分数值失败和物理解体:

> 计算并不是因为"星系解体"而停止,而是因为位置、速度或派生指标出现了计算机无法继续表示的数值.当前结果只能使用到失败之前.

失败详情继续展示 `bodyId`、`field`、`step`、`simulationTimeSeconds`、`value` 和原始消息.

## 四、物理现象解释

### 4.1 展示入口

在事件面板顶部增加"现象总览",位于逐条事件时间线之前;报告页完整复用同一套解释结构.

总览包含三部分:

- **已观察到**:近遇、可能逃逸、剧烈偏转和快速解体,包括相关天体、时间和直接证据.
- **可能解释**:后端诊断给出的假设,明确标记为待验证解释.
- **如何确认**:定位相关时间点或创建更小时间步长的对照实验.

### 4.2 证据与因果边界

现有诊断码表示检测到的运动特征,不直接等价于已经证明原因:

- `POSSIBLE_ESCAPE`:观察到天体向外运动且速度超过局部逃逸尺度.
- `SUDDEN_DEFLECTION`:观察到单步速度方向发生明显偏转.
- `RAPID_DISASSEMBLY`:观察到质量加权半径明显增大且多数天体向外运动.
- `NEAR_ENCOUNTER`:观察到天体距离进入近遇阈值.

界面统一把信息分为:

- "已观察到":可以由数据直接支持的事实.
- "可能相关":时间或天体范围上有关联,但尚未证明因果.
- "需要对照实验确认":需要改变时间步长重新运行才能进一步判断.

不生成未经证据支持的确定因果链,也不使用没有校准依据的概率或置信度百分比.

### 4.3 与 Health 的关系

- Health 为 `WARNING`、`POOR` 或 `FAILED` 时,明确说明当前数值误差限制了物理解读.
- Health 为 `GOOD` 时,只说明现象在当前项目数值容差内出现,不宣称已经验证现实物理模型.
- 近遇本身不是错误,逃逸也不等于程序崩溃.
- 数值失败不能被表述为真实星系解体.

技术性的 `causeCategory` 和诊断码移到技术详情,不在首层直接把事件标成"物理原因".同时修复事件证据中时间步长误用长度单位格式的问题.

## 五、可解释的 Clone & Retry

### 5.1 用户交互

主操作改称"创建对照实验",在技术详情中保留 Clone & Retry 术语.

点击后先打开确认面板,展示:

- 原始与建议时间步长.
- 原始与新最大步数.
- 保持不变的模拟覆盖时间.
- 预计积分步数和相对计算量倍数.
- 此次调整的目的.
- 调整的代价.
- 源运行记录会被保留.

确认后只把配置载入参数编辑器,不自动创建实验.用户仍需经过现有配置校验和 Warning 确认流程.

### 5.2 默认保持模拟时长

默认策略为保持相同模拟覆盖时间:

- `MAX_STEPS`:`newMaxSteps = ceil(oldMaxSteps × oldDt / newDt)`.
- `TARGET_TIME`:目标模拟时间保持不变.
- `BOTH`:目标模拟时间保持不变,同时按比例调整最大步数.

不估算墙钟运行时间,只展示步数和相对计算量倍数,避免对不同设备作不可靠承诺.

### 5.3 最大步数超限

如果保持模拟时长会使最大步数超过项目上限 `100,000,000`,系统不能静默截断或自行改变承诺,必须要求用户选择:

1. 保留建议时间步长,但缩短模拟覆盖时间.
2. 使用仍能保持原覆盖时间的最小合法时间步长调整幅度.
3. 取消并手动修改.

### 5.4 对照实验关系

每个通过建议创建的 Run 持久记录直接来源,并可追溯完整实验链:

- 队列显示"第 N 次对照实验".
- 详情和报告可以返回上一条来源记录.
- 可以追溯到最初 Run.
- 第一版不建设完整 Compare 页面,只展示来源关系、关键配置差异和最终 Health 对照.

如果用户在接受建议后又修改了其他初始条件,系统需要明确提示:结果差异不能只归因于时间步长变化.

删除源 Run 时不级联删除后代.后代保留来源 ID、来源名称和参数差异快照;链接不可用时显示"源运行记录已删除".

## 六、公共契约与数据模型

### 6.1 配置校验契约

继续使用现有 `POST /configs/validate`,不新增端点.

扩展 `ValidationResult`:

```text
configSummary:
  estimatedSteps
  estimatedSimulationTimeSeconds
  limitingEndCondition
  initialMinimumPairDistanceMeters
  softeningToInitialDistanceRatio
```

为 `ValidationIssue` 增加可空 `guidance`,旧消费者仍可继续读取原有 `message`:

```text
guidance:
  observation
  impact
  evidence[]
  primaryAction
  alternatives[]
```

`GuidanceEvidence` 至少包含:

- 证据码.
- SI 数值.
- 可空参考值或比例.
- 可空相关天体 ID.

`GuidanceAction` 包含:

```text
code
mode: APPLY_PATCH | MANUAL_REVIEW
label
rationale
tradeoff
configPatch?
adjustmentPolicy?: PRESERVE_SIMULATION_DURATION
```

后端负责风险证据、建议值、影响和通俗解释;前端只做单位转换与展示.

### 6.2 Health 契约

- 保持现有四状态、阈值和判级规则不变.
- 为 `HealthRecommendation` 增加可空的:
  - `rationale`
  - `tradeoff`
  - `verification`
- `HealthConfigPatch` 仍只自动建议数值控制项.
- WebSocket 继续使用 latest-wins `HEALTH` 消息,不新增消息类型.
- 新字段采用向后兼容的可选字段;如 Schema 版本策略要求,升级 Schema 并让前端在过渡期兼容旧版本.

### 6.3 创建实验与来源关系

扩展现有 `ExperimentCreateRequest`,增加可空 `retryContext`:

```text
sourceExperimentId
recommendationCode
strategy: PRESERVE_SIMULATION_DURATION | PRESERVE_STEP_COUNT
```

服务端必须:

- 检查源 Run 是否存在.
- 检查 recommendation code 是否来自源 Run 当前 Health.
- 不信任客户端提交的派生来源深度和差异快照.
- 根据源 Run 与最终规范化配置生成持久化来源记录.

持久化并通过列表、详情和报告返回:

```text
sourceExperimentId
sourceExperimentName
rootExperimentId
retryDepth
recommendationCode
strategy
before/after timeStepSeconds
before/after end conditions
before/after estimated duration
before/after estimated steps
changedFields
```

旧持久化文件缺少来源字段时按 `null` 读取,不进行迁移回填.

## 七、前端与 Mock

- 抽取统一的参数说明组件,避免说明文案散落在模板中.
- 抽取共享的对照实验规划函数,统一处理三种结束条件、步数倍数和超限判断.
- 参数编辑器、Warning 弹窗、Health、现象总览和报告使用中文主称:
  - Run → 运行记录.
  - Health → 数值健康或数值结果可信度.
  - Clone & Retry → 创建对照实验.
- 英文术语只在首次出现或技术详情中保留.
- 静态原理由前端集中维护;当前天体名称、比例、距离、证据和建议值来自后端结构化数据.
- 事件总览只组合已有权威事件与 Health,不在前端新增物理或数值阈值.
- 离线 mock 实现相同契约和可控测试数据,但生产 UI 不导入 mock 规则.

## 八、测试与验收

### 8.1 配置校验

- 安全配置只返回事实摘要,不主动显示风险建议.
- 时间步长 CAUTION/HIGH 返回正确证据、首选 patch 和代价.
- 软化长度、速度和位置风险只返回人工调整建议.
- 自动校验覆盖防抖、输入失效、乱序响应和提交前强制重校验.
- SI 契约与前端单位转换保持一致.

### 8.2 对照实验

- `MAX_STEPS`、`TARGET_TIME`、`BOTH` 三种结束条件均正确保持模拟时长.
- 原/新步数及计算成本倍数正确.
- 超过 1 亿步时必须由用户选择,不能静默截断.
- 新 Run ID 与源 Run 不同,源 Run 保留.
- 完整重试链在重启后可以恢复.
- 旧 manifest、源 Run 被删除和多次重试正确降级展示.
- 用户额外修改初始条件时正确提示归因限制.

### 8.3 Health

- 四种状态首层文案与后端状态一致.
- 结论、证据、验证方法和技术详情分层正确.
- FAILED 明确区别数值失败与物理解体.
- 前端不存在 Health 阈值判断.
- 回放模式继续说明 Health 代表整个 Run 截至最新状态.

### 8.4 现象总览

- 近遇、逃逸、偏转和快速解体只作为已观察事实展示.
- Health 降级时展示物理解读限制.
- 可能原因明确标为待验证,不生成确定因果.
- 实验室和报告使用一致解释.
- 时间步长、软化长度、距离、比例和模拟时间使用正确单位格式.

### 8.5 E2E

- 修改风险参数后自动出现字段解释.
- Warning 弹窗能够应用建议或按原配置运行.
- Health 降级后完成"创建对照实验 → 检查参数差异 → 创建新 Run".
- 新 Run ID 不同、源 Run 保留且来源链可见.
- 报告能够查看来源关系、数值可信度解释和现象总览.
- 1920×1080 及现有响应式尺寸不产生横向溢出.

### 8.6 文案验收

本轮采用内部评审,不安排真实目标用户任务测试.评审必须检查:

- 科学准确性.
- 已观察事实与原因推测是否分开.
- 是否解释术语而不回避术语.
- 建议是否说明收益和代价.
- 是否避免居高临下、幼稚比喻和过度安抚.
- 是否避免把启发式判断写成确定事实.

## 九、实施顺序

1. 扩展配置校验、Health 建议和来源关系的领域模型与共享契约.
2. 实现后端结构化配置解读、风险证据和建议动作.
3. 实现对照实验规划、来源持久化和 REST 返回.
4. 改造参数编辑器和 Warning 弹窗.
5. 改造 Health 为"结论 → 证据 → 怎么验证 → 技术详情".
6. 实现事件面板现象总览并在报告页复用.
7. 接入离线 mock、生成契约类型并补全测试.
8. 执行分层测试、前端构建、E2E、全量 Maven 验证和最终 diff 审阅.

## 十、明确假设与非目标

- 不新增体验模式或专业模式.
- 不新增或重做场景卡片.
- 不建设完整 Compare 页面.
- 不开放用户编辑 Health 阈值.
- 不自动修改软化长度、位置、速度、质量或引力常数.
- 不估算设备墙钟运行时间.
- 不把存储、网络或文件错误标为 Numerical Health FAILED.
- 本轮不更换现有物理诊断算法,只修正语义呈现和明显的单位展示问题.
- 后端继续作为配置风险、Health 状态和诊断证据的权威来源.
- 实施完成后同步更新 `GUIDED_PARAMETER_EXPERIENCE.md`,使建议文档与最终产品行为一致.

## 十一、已确认的产品决策记录

- 首版范围:完整调试闭环,事件解释做到"解释证据但不编造因果".
- 用户目标:能够理解并自行调参.
- 参数说明:短句常驻并可展开.
- 参数范围:全部参数分层覆盖.
- 建议触发:只在检测到风险时主动出现.
- 建议组织:一个首选方案加替代方案.
- 风险落点:字段旁和 Warning 确认弹窗.
- Health 首层:数值结果可信度.
- 重试默认:保持模拟时长.
- 解释权威:后端返回结构化解释.
- 来源关系:持久记录并保留完整实验链.
- 原配置运行:理解风险后仍然允许.
- 自动 patch:只修改数值控制项.
- 第一版对照:显示来源和关键差异,不建设完整 Compare.
- 自动校验:停止输入后防抖更新.
- 最大步数超限:要求用户重新选择.
- 成本表达:步数和相对倍数.
- 判断强度:事实与推测分开.
- 现象总览位置:事件面板顶部.
- 报告:完整复用解释与来源关系.
- 文案语气:平实尊重、证据优先.
- 术语:中文主称、英文辅称.
- 动态文案:静态原理结合当前配置证据.
- 验收:自动化测试加内部文案评审.
