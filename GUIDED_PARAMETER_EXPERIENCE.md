# Three Body Lab 参数引导与数值诊断体验建议

更新时间:2026-08-13

## 背景

Three Body Lab 的目标用户可能不具备扎实的物理或数学基础,但这不代表他们缺乏理解能力.用户真正缺少的是系统提供的上下文:参数控制了什么、不同参数如何相互影响、模拟为什么失败、天体系统为什么发生解体,以及应当如何通过新的实验验证判断.

因此,产品方向不应是隐藏专业概念或把用户划分为"体验模式"和"专业模式",也不应把所有决策交给一个不可解释的自动配置.更合适的原则是:

> 保持一套完整界面,在用户需要作出决定的位置,用准确、通俗、有因果关系的语言解释参数和诊断,并允许用户通过对照实验验证系统建议.

项目已经通过初始配置提供场景选择,本方案不重复建设场景卡片.

## 设计原则

1. 不回避物理和数值概念,但避免只给术语定义.
2. 参数说明必须回答"控制什么、设置不当会怎样、何时应该调整".
3. 明确区分物理现象、数值误差和暂时无法判断的情况.
4. 系统建议应附带依据、预期收益和代价,不能宣称建议值是正确或最优解.
5. 鼓励用户保留原实验并创建对照实验,通过结果差异理解参数影响.
6. 使用渐进式信息展示:首先给出结论和行动建议,需要时再展开证据和技术细节.

## 建议一:为参数提供"作用—风险—调整方法"引导

参数编辑器不应只显示字段名、单位和一句名词解释.每个关键参数应提供三层信息:

- 它控制什么.
- 设置不合适可能造成什么结果.
- 观察到什么现象时应当调整.

### 时间步长

建议文案:

> 模拟每次向前推进多长时间.步长越小,越容易捕捉快速变化,但需要计算更多次.如果天体近距离掠过时轨迹突然跳跃,或数值健康持续恶化,可以减小步长后重新运行.

时间步长不是单纯的"精度"选项.其是否合适取决于系统运动变化的速度,尤其是近距离相遇期间的快速变化.

### 软化长度

建议文案:

> 用于限制天体极近距离时引力计算的剧烈增长,避免一次近距离接触产生极端加速度.太小可能导致计算不稳定;太大则可能把本应明显的近距离引力效果变得过于平缓.它不是简单的"越大越稳定、越小越准确".

### 最大步数

建议文案:

> 决定模拟最多计算多少次.它和时间步长共同决定模拟能走多远:预计模拟时长 = 时间步长 × 最大步数.

### 适用前提

- 继续保留当前单一参数编辑器.
- 前端能够统一显示单位.
- 系统可以根据当前配置动态计算预计模拟时长.
- 参数说明使用统一组件,避免同一概念在不同页面出现冲突文案.

## 建议二:增加运行前的"配置解读"

现有参数校验主要回答"能不能运行".运行前还应回答"这组参数意味着什么".

用户修改配置时,可以实时生成摘要,例如:

> 本实验每一步推进 5000 秒,最多运行 10,000 步,相当于约 1.58 年的模拟时间.当前步长偏向快速预览;如果发生近距离相遇,可能无法充分解析轨迹变化.软化长度约为初始最近天体距离的 0.3%,通常只会明显影响非常接近的过程.

提示建议划分为三类:

- **信息**:解释配置代表的时间范围、距离比例和计算规模.
- **注意**:说明配置可能影响哪些现象,以及如何验证.
- **阻止运行**:仅用于明确非法或必然无法计算的配置.

系统不应只显示"时间步长过大",而应说明它相对于什么尺度偏大,以及可能造成的可观察后果.

### 适用前提

- 可以从初始配置可靠计算最近距离、预计模拟时长和总计算规模.
- 如果需要评价轨道周期或动力学时间尺度,必须先建立可靠的尺度估计方法.
- 无法可靠推导的结论使用中性说明,不使用拍脑袋的通用阈值.

## 建议三:将 Simulation Health 改造成完整的故障解释

Health 不应只是状态颜色和指标列表.展开后应按固定顺序回答:

1. 发生了什么.
2. 系统为什么这样判断.
3. 这更像物理现象还是数值问题.
4. 用户下一步可以怎样验证.

### WARNING 示例

> 结果需要谨慎使用.在第 4280 步附近,能量漂移达到 0.54%,超过项目默认的 0.1% 提醒线.同一时间附近发生了一次近距离相遇.近距离相遇本身是真实物理现象,但当前时间步长可能不足以解析这段快速变化.建议创建一个副本,将时间步长从 5000 秒调整为 500 秒,再比较两次运行的轨迹.

### FAILED 示例

> 计算并不是因为"星系解体"而停止,而是因为某个位置或速度增长为计算机无法表示的数值.当前结果只能使用到失败之前.

技术详情可以继续显示漂移峰值、阈值、步数、模拟时间、异常天体和异常字段,但应放在结论与行动建议之后.

### 适用前提

- 后端继续作为 Health 判级的唯一权威.
- Health 保留峰值、趋势、失败字段和相关近遇.
- 物理诊断事件与数值健康使用不同的 reason code 和展示语言.
- 时间邻近只能描述为相关线索,不能直接宣称存在因果关系.

## 建议四:将 Clone & Retry 设计为可解释的参数实验

Clone & Retry 不应表现为自动修复按钮,而应解释即将进行的实验:

> 建议创建一个对照实验:时间步长从 5000 秒调整为 500 秒.这样可以增加近距离相遇期间的计算分辨率,但达到相同模拟时长大约需要 10 倍计算步骤.源实验将被保留,方便比较结果.

如果实验仅设置最大步数,那么将时间步长降低 10 倍并保持最大步数不变,会使新实验只能模拟原来十分之一的时间.创建副本时应让用户选择:

- **保持相同模拟时长**:按比例增加最大步数,计算成本上升.
- **保持相同步数**:计算规模接近不变,但模拟覆盖时间缩短.

系统应展示变更前后参数、变更原因、预计影响和计算代价.用户确认后仍通过现有参数校验与 Warning 确认流程创建新 Run.

### 适用前提

- Clone 创建新 Run ID,并保留源 Run.
- 系统能够可靠计算 `timeStepSeconds × maxSteps` 的变化.
- 推荐配置仍需经过现有配置校验.
- 未来若支持结果比较,可以直接将源 Run 与重试 Run 建立关联.

## 建议五:用事件时间线解释"星系为什么解体"

用户看到天体飞散时,需要了解发生过程,而不只是看到"解体"标签.系统可以利用已有事件构造简洁的现象链:

```text
近距离相遇
  → 天体速度和运动方向快速改变
  → 一个天体获得较高速度并持续远离系统
  → 系统不再保持原有结构
```

展示结论时必须区分:

- **物理解体**:数值健康仍然良好,但天体确实发生逃逸或系统结构瓦解.
- **可能存在数值假象**:解体附近漂移明显升高,当前步长可能不足以解析快速变化.
- **暂时无法判断**:事件只是在时间上相邻,需要更小时间步长的对照实验.

近遇本身不是错误,逃逸也不等于程序崩溃.反过来,数值失败也不能被解释成真实的星系解体.

### 适用前提

- 近遇、逃逸、偏转和解体事件拥有可靠的步数、模拟时间和天体 ID.
- 事件时间线能够关联同一组天体.
- 没有足够证据时,界面明确使用"可能""时间上相邻"和"建议对照实验".

## 建议实施顺序

1. 参数"作用—风险—调整方法"说明.
2. Health 的通俗结论、证据和行动建议分层.
3. Clone & Retry 的参数差异、模拟时长和计算代价说明.
4. 运行前配置解读.
5. 基于事件的物理现象时间线.

前三项可以优先利用现有参数编辑器、Simulation Health 和 Clone & Retry 基础,修改范围相对明确.运行前尺度评价和物理事件叙事涉及更多物理判断,应在规则和证据模型确认后实施.

## 当前实现结论

- 参数引导采用"常驻短句 + 按需展开",Tooltip 只保留为辅助.
- 配置事实常驻参数区;风险解释同时出现在对应字段附近和 Warning 确认弹窗.
- Health 首层只给数值可信度结论,展开后依次展示证据、验证方法和技术详情.
- "创建对照实验"默认保持模拟覆盖时间;若超过一亿步,必须由用户重新选择,不静默截断.
- 现象总览只陈述已观察到的运动特征,把原因写成待验证解释;数值健康降级时明确限制物理解读.
- 对照实验来源、差异快照和最终 Health 可追溯,源运行记录删除后不影响后代数据.

具体契约、测试和当前完成情况见 `GUIDED_PARAMETER_IMPLEMENTATION_PLAN.md` 与 `GUIDED_PARAMETER_PROGRESS.md`.
